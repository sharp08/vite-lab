import { defineComponent } from "vue";

import ms from "./RouteGallery.module.less";

export default defineComponent({
  name: "RouteGallery",
  setup(props, ctx) {
    return () => <div>RouteGallery</div>;
  }
});
